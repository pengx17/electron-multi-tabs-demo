{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "../lib/helper",
    "composite": true,
    "types": ["node", "electron"],
    "lib": [] // Remove the default DOM libs
  },
  "include": ["./**/*.ts"]
}
